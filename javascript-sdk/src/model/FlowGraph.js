/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AbstractGraph from './AbstractGraph';
import FlowGraphCluster from './FlowGraphCluster';
import FlowGraphEdge from './FlowGraphEdge';

/**
 * The FlowGraph model module.
 * @module model/FlowGraph
 * @version 1.0.0-beta5
 */
class FlowGraph {
    /**
     * Constructs a new <code>FlowGraph</code>.
     * @alias module:model/FlowGraph
     * @param nodes {Array.<module:model/AbstractGraph>} 
     * @param edges {Array.<module:model/FlowGraphEdge>} 
     * @param clusters {Array.<module:model/FlowGraphCluster>} 
     * @param flowables {Array.<String>} 
     */
    constructor(nodes, edges, clusters, flowables) { 
        
        FlowGraph.initialize(this, nodes, edges, clusters, flowables);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, nodes, edges, clusters, flowables) { 
        obj['nodes'] = nodes;
        obj['edges'] = edges;
        obj['clusters'] = clusters;
        obj['flowables'] = flowables;
    }

    /**
     * Constructs a <code>FlowGraph</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FlowGraph} obj Optional instance to populate.
     * @return {module:model/FlowGraph} The populated <code>FlowGraph</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FlowGraph();

            if (data.hasOwnProperty('nodes')) {
                obj['nodes'] = ApiClient.convertToType(data['nodes'], [AbstractGraph]);
            }
            if (data.hasOwnProperty('edges')) {
                obj['edges'] = ApiClient.convertToType(data['edges'], [FlowGraphEdge]);
            }
            if (data.hasOwnProperty('clusters')) {
                obj['clusters'] = ApiClient.convertToType(data['clusters'], [FlowGraphCluster]);
            }
            if (data.hasOwnProperty('flowables')) {
                obj['flowables'] = ApiClient.convertToType(data['flowables'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FlowGraph</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FlowGraph</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FlowGraph.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['nodes']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['nodes'])) {
                throw new Error("Expected the field `nodes` to be an array in the JSON data but got " + data['nodes']);
            }
            // validate the optional field `nodes` (array)
            for (const item of data['nodes']) {
                AbstractGraph.validateJSON(item);
            };
        }
        if (data['edges']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['edges'])) {
                throw new Error("Expected the field `edges` to be an array in the JSON data but got " + data['edges']);
            }
            // validate the optional field `edges` (array)
            for (const item of data['edges']) {
                FlowGraphEdge.validateJSON(item);
            };
        }
        if (data['clusters']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['clusters'])) {
                throw new Error("Expected the field `clusters` to be an array in the JSON data but got " + data['clusters']);
            }
            // validate the optional field `clusters` (array)
            for (const item of data['clusters']) {
                FlowGraphCluster.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['flowables'])) {
            throw new Error("Expected the field `flowables` to be an array in the JSON data but got " + data['flowables']);
        }

        return true;
    }


}

FlowGraph.RequiredProperties = ["nodes", "edges", "clusters", "flowables"];

/**
 * @member {Array.<module:model/AbstractGraph>} nodes
 */
FlowGraph.prototype['nodes'] = undefined;

/**
 * @member {Array.<module:model/FlowGraphEdge>} edges
 */
FlowGraph.prototype['edges'] = undefined;

/**
 * @member {Array.<module:model/FlowGraphCluster>} clusters
 */
FlowGraph.prototype['clusters'] = undefined;

/**
 * @member {Array.<String>} flowables
 */
FlowGraph.prototype['flowables'] = undefined;






export default FlowGraph;

