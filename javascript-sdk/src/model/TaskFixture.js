/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PropertyString from './PropertyString';
import StateType from './StateType';

/**
 * The TaskFixture model module.
 * @module model/TaskFixture
 * @version v0.24.0
 */
class TaskFixture {
    /**
     * Constructs a new <code>TaskFixture</code>.
     * @alias module:model/TaskFixture
     * @param id {String} 
     */
    constructor(id) { 
        
        TaskFixture.initialize(this, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id) { 
        obj['id'] = id;
    }

    /**
     * Constructs a <code>TaskFixture</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TaskFixture} obj Optional instance to populate.
     * @return {module:model/TaskFixture} The populated <code>TaskFixture</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TaskFixture();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = StateType.constructFromObject(data['state']);
            }
            if (data.hasOwnProperty('outputs')) {
                obj['outputs'] = ApiClient.convertToType(data['outputs'], {'String': Object});
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = PropertyString.constructFromObject(data['description']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TaskFixture</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TaskFixture</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TaskFixture.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }
        // validate the optional field `description`
        if (data['description']) { // data not null
          PropertyString.validateJSON(data['description']);
        }

        return true;
    }


}

TaskFixture.RequiredProperties = ["id"];

/**
 * @member {String} id
 */
TaskFixture.prototype['id'] = undefined;

/**
 * @member {String} value
 */
TaskFixture.prototype['value'] = undefined;

/**
 * @member {module:model/StateType} state
 */
TaskFixture.prototype['state'] = undefined;

/**
 * @member {Object.<String, Object>} outputs
 */
TaskFixture.prototype['outputs'] = undefined;

/**
 * @member {module:model/PropertyString} description
 */
TaskFixture.prototype['description'] = undefined;






export default TaskFixture;

