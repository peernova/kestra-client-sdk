/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ApiSecretMeta from './ApiSecretMeta';

/**
 * The ApiSecretListResponse model module.
 * @module model/ApiSecretListResponse
 * @version 1.0.0-beta5
 */
class ApiSecretListResponse {
    /**
     * Constructs a new <code>ApiSecretListResponse</code>.
     * @alias module:model/ApiSecretListResponse
     * @param readOnly {Boolean} 
     * @param results {Array.<module:model/ApiSecretMeta>} 
     * @param total {Number} 
     */
    constructor(readOnly, results, total) { 
        
        ApiSecretListResponse.initialize(this, readOnly, results, total);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, readOnly, results, total) { 
        obj['readOnly'] = readOnly;
        obj['results'] = results;
        obj['total'] = total;
    }

    /**
     * Constructs a <code>ApiSecretListResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiSecretListResponse} obj Optional instance to populate.
     * @return {module:model/ApiSecretListResponse} The populated <code>ApiSecretListResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiSecretListResponse();

            if (data.hasOwnProperty('readOnly')) {
                obj['readOnly'] = ApiClient.convertToType(data['readOnly'], 'Boolean');
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [ApiSecretMeta]);
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiSecretListResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiSecretListResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiSecretListResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                ApiSecretMeta.validateJSON(item);
            };
        }

        return true;
    }


}

ApiSecretListResponse.RequiredProperties = ["readOnly", "results", "total"];

/**
 * @member {Boolean} readOnly
 */
ApiSecretListResponse.prototype['readOnly'] = undefined;

/**
 * @member {Array.<module:model/ApiSecretMeta>} results
 */
ApiSecretListResponse.prototype['results'] = undefined;

/**
 * @member {Number} total
 */
ApiSecretListResponse.prototype['total'] = undefined;






export default ApiSecretListResponse;

