/**
 * Kestra EE
 * All API operations allow an optional tenant identifier in the HTTP path, if you don't use multi-tenancy you must omit the tenant identifier.<br/> This means that, for example, when trying to access the Flows API, instead of using <code>/api/v1/{tenant}/flows</code> you must use <code>/api/v1/flows</code>.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AbstractUserGroupIdentifier from './AbstractUserGroupIdentifier';

/**
 * The ApiServiceAccount model module.
 * @module model/ApiServiceAccount
 * @version v1
 */
class ApiServiceAccount {
    /**
     * Constructs a new <code>ApiServiceAccount</code>.
     * A User Service Account.
     * @alias module:model/ApiServiceAccount
     * @param name {String} the name of this service account.
     */
    constructor(name) { 
        
        ApiServiceAccount.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>ApiServiceAccount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiServiceAccount} obj Optional instance to populate.
     * @return {module:model/ApiServiceAccount} The populated <code>ApiServiceAccount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiServiceAccount();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('groupList')) {
                obj['groupList'] = ApiClient.convertToType(data['groupList'], [AbstractUserGroupIdentifier]);
            }
            if (data.hasOwnProperty('isSuperAdmin')) {
                obj['isSuperAdmin'] = ApiClient.convertToType(data['isSuperAdmin'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiServiceAccount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiServiceAccount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiServiceAccount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['groupList']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groupList'])) {
                throw new Error("Expected the field `groupList` to be an array in the JSON data but got " + data['groupList']);
            }
            // validate the optional field `groupList` (array)
            for (const item of data['groupList']) {
                AbstractUserGroupIdentifier.validateJSON(item);
            };
        }

        return true;
    }


}

ApiServiceAccount.RequiredProperties = ["name"];

/**
 * the identifier of this service account.
 * @member {String} id
 */
ApiServiceAccount.prototype['id'] = undefined;

/**
 * the name of this service account.
 * @member {String} name
 */
ApiServiceAccount.prototype['name'] = undefined;

/**
 * the description of this service account.
 * @member {String} description
 */
ApiServiceAccount.prototype['description'] = undefined;

/**
 * @member {Array.<module:model/AbstractUserGroupIdentifier>} groupList
 */
ApiServiceAccount.prototype['groupList'] = undefined;

/**
 * @member {Boolean} isSuperAdmin
 */
ApiServiceAccount.prototype['isSuperAdmin'] = undefined;






export default ApiServiceAccount;

