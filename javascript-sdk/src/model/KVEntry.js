/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The KVEntry model module.
 * @module model/KVEntry
 * @version 1.0.0
 */
class KVEntry {
    /**
     * Constructs a new <code>KVEntry</code>.
     * @alias module:model/KVEntry
     * @param key {String} 
     * @param creationDate {Date} 
     * @param updateDate {Date} 
     */
    constructor(key, creationDate, updateDate) { 
        
        KVEntry.initialize(this, key, creationDate, updateDate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, key, creationDate, updateDate) { 
        obj['key'] = key;
        obj['creationDate'] = creationDate;
        obj['updateDate'] = updateDate;
    }

    /**
     * Constructs a <code>KVEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KVEntry} obj Optional instance to populate.
     * @return {module:model/KVEntry} The populated <code>KVEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KVEntry();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('creationDate')) {
                obj['creationDate'] = ApiClient.convertToType(data['creationDate'], 'Date');
            }
            if (data.hasOwnProperty('updateDate')) {
                obj['updateDate'] = ApiClient.convertToType(data['updateDate'], 'Date');
            }
            if (data.hasOwnProperty('expirationDate')) {
                obj['expirationDate'] = ApiClient.convertToType(data['expirationDate'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KVEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KVEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KVEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }

        return true;
    }


}

KVEntry.RequiredProperties = ["key", "creationDate", "updateDate"];

/**
 * @member {String} key
 */
KVEntry.prototype['key'] = undefined;

/**
 * @member {String} description
 */
KVEntry.prototype['description'] = undefined;

/**
 * @member {Date} creationDate
 */
KVEntry.prototype['creationDate'] = undefined;

/**
 * @member {Date} updateDate
 */
KVEntry.prototype['updateDate'] = undefined;

/**
 * @member {Date} expirationDate
 */
KVEntry.prototype['expirationDate'] = undefined;






export default KVEntry;

