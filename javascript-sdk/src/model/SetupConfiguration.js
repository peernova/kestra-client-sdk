/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SetupConfiguration model module.
 * @module model/SetupConfiguration
 * @version 1.0.0
 */
class SetupConfiguration {
    /**
     * Constructs a new <code>SetupConfiguration</code>.
     * @alias module:model/SetupConfiguration
     * @param done {Boolean} 
     * @param passwordRegexp {String} 
     */
    constructor(done, passwordRegexp) { 
        
        SetupConfiguration.initialize(this, done, passwordRegexp);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, done, passwordRegexp) { 
        obj['done'] = done;
        obj['passwordRegexp'] = passwordRegexp;
    }

    /**
     * Constructs a <code>SetupConfiguration</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SetupConfiguration} obj Optional instance to populate.
     * @return {module:model/SetupConfiguration} The populated <code>SetupConfiguration</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SetupConfiguration();

            if (data.hasOwnProperty('done')) {
                obj['done'] = ApiClient.convertToType(data['done'], 'Boolean');
            }
            if (data.hasOwnProperty('repositoryType')) {
                obj['repositoryType'] = ApiClient.convertToType(data['repositoryType'], 'String');
            }
            if (data.hasOwnProperty('queueType')) {
                obj['queueType'] = ApiClient.convertToType(data['queueType'], 'String');
            }
            if (data.hasOwnProperty('storageType')) {
                obj['storageType'] = ApiClient.convertToType(data['storageType'], 'String');
            }
            if (data.hasOwnProperty('secretType')) {
                obj['secretType'] = ApiClient.convertToType(data['secretType'], 'String');
            }
            if (data.hasOwnProperty('passwordRegexp')) {
                obj['passwordRegexp'] = ApiClient.convertToType(data['passwordRegexp'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SetupConfiguration</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SetupConfiguration</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SetupConfiguration.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['repositoryType'] && !(typeof data['repositoryType'] === 'string' || data['repositoryType'] instanceof String)) {
            throw new Error("Expected the field `repositoryType` to be a primitive type in the JSON string but got " + data['repositoryType']);
        }
        // ensure the json data is a string
        if (data['queueType'] && !(typeof data['queueType'] === 'string' || data['queueType'] instanceof String)) {
            throw new Error("Expected the field `queueType` to be a primitive type in the JSON string but got " + data['queueType']);
        }
        // ensure the json data is a string
        if (data['storageType'] && !(typeof data['storageType'] === 'string' || data['storageType'] instanceof String)) {
            throw new Error("Expected the field `storageType` to be a primitive type in the JSON string but got " + data['storageType']);
        }
        // ensure the json data is a string
        if (data['secretType'] && !(typeof data['secretType'] === 'string' || data['secretType'] instanceof String)) {
            throw new Error("Expected the field `secretType` to be a primitive type in the JSON string but got " + data['secretType']);
        }
        // ensure the json data is a string
        if (data['passwordRegexp'] && !(typeof data['passwordRegexp'] === 'string' || data['passwordRegexp'] instanceof String)) {
            throw new Error("Expected the field `passwordRegexp` to be a primitive type in the JSON string but got " + data['passwordRegexp']);
        }

        return true;
    }


}

SetupConfiguration.RequiredProperties = ["done", "passwordRegexp"];

/**
 * @member {Boolean} done
 */
SetupConfiguration.prototype['done'] = undefined;

/**
 * @member {String} repositoryType
 */
SetupConfiguration.prototype['repositoryType'] = undefined;

/**
 * @member {String} queueType
 */
SetupConfiguration.prototype['queueType'] = undefined;

/**
 * @member {String} storageType
 */
SetupConfiguration.prototype['storageType'] = undefined;

/**
 * @member {String} secretType
 */
SetupConfiguration.prototype['secretType'] = undefined;

/**
 * @member {String} passwordRegexp
 */
SetupConfiguration.prototype['passwordRegexp'] = undefined;






export default SetupConfiguration;

