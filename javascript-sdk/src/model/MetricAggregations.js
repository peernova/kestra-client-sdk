/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MetricAggregation from './MetricAggregation';

/**
 * The MetricAggregations model module.
 * @module model/MetricAggregations
 * @version 1.0.0-beta5
 */
class MetricAggregations {
    /**
     * Constructs a new <code>MetricAggregations</code>.
     * @alias module:model/MetricAggregations
     * @param groupBy {String} 
     * @param aggregations {Array.<module:model/MetricAggregation>} 
     */
    constructor(groupBy, aggregations) { 
        
        MetricAggregations.initialize(this, groupBy, aggregations);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, groupBy, aggregations) { 
        obj['groupBy'] = groupBy;
        obj['aggregations'] = aggregations;
    }

    /**
     * Constructs a <code>MetricAggregations</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MetricAggregations} obj Optional instance to populate.
     * @return {module:model/MetricAggregations} The populated <code>MetricAggregations</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MetricAggregations();

            if (data.hasOwnProperty('groupBy')) {
                obj['groupBy'] = ApiClient.convertToType(data['groupBy'], 'String');
            }
            if (data.hasOwnProperty('aggregations')) {
                obj['aggregations'] = ApiClient.convertToType(data['aggregations'], [MetricAggregation]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MetricAggregations</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MetricAggregations</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MetricAggregations.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['groupBy'] && !(typeof data['groupBy'] === 'string' || data['groupBy'] instanceof String)) {
            throw new Error("Expected the field `groupBy` to be a primitive type in the JSON string but got " + data['groupBy']);
        }
        if (data['aggregations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['aggregations'])) {
                throw new Error("Expected the field `aggregations` to be an array in the JSON data but got " + data['aggregations']);
            }
            // validate the optional field `aggregations` (array)
            for (const item of data['aggregations']) {
                MetricAggregation.validateJSON(item);
            };
        }

        return true;
    }


}

MetricAggregations.RequiredProperties = ["groupBy", "aggregations"];

/**
 * @member {String} groupBy
 */
MetricAggregations.prototype['groupBy'] = undefined;

/**
 * @member {Array.<module:model/MetricAggregation>} aggregations
 */
MetricAggregations.prototype['aggregations'] = undefined;






export default MetricAggregations;

