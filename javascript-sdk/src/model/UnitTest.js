/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Assertion from './Assertion';
import Fixtures from './Fixtures';

/**
 * The UnitTest model module.
 * @module model/UnitTest
 * @version v0.24.0
 */
class UnitTest {
    /**
     * Constructs a new <code>UnitTest</code>.
     * @alias module:model/UnitTest
     * @param id {String} 
     * @param type {String} 
     * @param assertions {Array.<module:model/Assertion>} 
     */
    constructor(id, type, assertions) { 
        
        UnitTest.initialize(this, id, type, assertions);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, type, assertions) { 
        obj['id'] = id;
        obj['type'] = type;
        obj['assertions'] = assertions;
    }

    /**
     * Constructs a <code>UnitTest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UnitTest} obj Optional instance to populate.
     * @return {module:model/UnitTest} The populated <code>UnitTest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UnitTest();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('disabled')) {
                obj['disabled'] = ApiClient.convertToType(data['disabled'], 'Boolean');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('fixtures')) {
                obj['fixtures'] = Fixtures.constructFromObject(data['fixtures']);
            }
            if (data.hasOwnProperty('assertions')) {
                obj['assertions'] = ApiClient.convertToType(data['assertions'], [Assertion]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UnitTest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UnitTest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UnitTest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // validate the optional field `fixtures`
        if (data['fixtures']) { // data not null
          Fixtures.validateJSON(data['fixtures']);
        }
        if (data['assertions']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assertions'])) {
                throw new Error("Expected the field `assertions` to be an array in the JSON data but got " + data['assertions']);
            }
            // validate the optional field `assertions` (array)
            for (const item of data['assertions']) {
                Assertion.validateJSON(item);
            };
        }

        return true;
    }


}

UnitTest.RequiredProperties = ["id", "type", "assertions"];

/**
 * @member {String} id
 */
UnitTest.prototype['id'] = undefined;

/**
 * @member {String} type
 */
UnitTest.prototype['type'] = undefined;

/**
 * @member {Boolean} disabled
 */
UnitTest.prototype['disabled'] = undefined;

/**
 * @member {String} description
 */
UnitTest.prototype['description'] = undefined;

/**
 * @member {module:model/Fixtures} fixtures
 */
UnitTest.prototype['fixtures'] = undefined;

/**
 * @member {Array.<module:model/Assertion>} assertions
 */
UnitTest.prototype['assertions'] = undefined;






export default UnitTest;

