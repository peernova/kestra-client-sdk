/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ServiceInstance from './ServiceInstance';

/**
 * The InstanceControllerApiWorkerGroupDetails model module.
 * @module model/InstanceControllerApiWorkerGroupDetails
 * @version 1.0.0
 */
class InstanceControllerApiWorkerGroupDetails {
    /**
     * Constructs a new <code>InstanceControllerApiWorkerGroupDetails</code>.
     * ApiWorkerGroupDetails.
     * @alias module:model/InstanceControllerApiWorkerGroupDetails
     * @param id {String} The ID of worker group.
     * @param key {String} The key of the worker group.
     * @param description {String} The description of the worker group.
     * @param allowedTenants {Array.<String>} 
     * @param workers {Array.<module:model/ServiceInstance>} The list of workers for the group.
     */
    constructor(id, key, description, allowedTenants, workers) { 
        
        InstanceControllerApiWorkerGroupDetails.initialize(this, id, key, description, allowedTenants, workers);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, key, description, allowedTenants, workers) { 
        obj['id'] = id;
        obj['key'] = key;
        obj['description'] = description;
        obj['allowedTenants'] = allowedTenants;
        obj['workers'] = workers;
    }

    /**
     * Constructs a <code>InstanceControllerApiWorkerGroupDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InstanceControllerApiWorkerGroupDetails} obj Optional instance to populate.
     * @return {module:model/InstanceControllerApiWorkerGroupDetails} The populated <code>InstanceControllerApiWorkerGroupDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InstanceControllerApiWorkerGroupDetails();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('allowedTenants')) {
                obj['allowedTenants'] = ApiClient.convertToType(data['allowedTenants'], ['String']);
            }
            if (data.hasOwnProperty('workers')) {
                obj['workers'] = ApiClient.convertToType(data['workers'], [ServiceInstance]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InstanceControllerApiWorkerGroupDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InstanceControllerApiWorkerGroupDetails</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InstanceControllerApiWorkerGroupDetails.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['allowedTenants'])) {
            throw new Error("Expected the field `allowedTenants` to be an array in the JSON data but got " + data['allowedTenants']);
        }
        if (data['workers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['workers'])) {
                throw new Error("Expected the field `workers` to be an array in the JSON data but got " + data['workers']);
            }
            // validate the optional field `workers` (array)
            for (const item of data['workers']) {
                ServiceInstance.validateJSON(item);
            };
        }

        return true;
    }


}

InstanceControllerApiWorkerGroupDetails.RequiredProperties = ["id", "key", "description", "allowedTenants", "workers"];

/**
 * The ID of worker group.
 * @member {String} id
 */
InstanceControllerApiWorkerGroupDetails.prototype['id'] = undefined;

/**
 * The key of the worker group.
 * @member {String} key
 */
InstanceControllerApiWorkerGroupDetails.prototype['key'] = undefined;

/**
 * The description of the worker group.
 * @member {String} description
 */
InstanceControllerApiWorkerGroupDetails.prototype['description'] = undefined;

/**
 * @member {Array.<String>} allowedTenants
 */
InstanceControllerApiWorkerGroupDetails.prototype['allowedTenants'] = undefined;

/**
 * The list of workers for the group.
 * @member {Array.<module:model/ServiceInstance>} workers
 */
InstanceControllerApiWorkerGroupDetails.prototype['workers'] = undefined;






export default InstanceControllerApiWorkerGroupDetails;

