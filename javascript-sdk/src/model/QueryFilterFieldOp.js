/**
 * Kestra EE
 * All API operations, except for Superadmin-only endpoints, require a tenant identifier in the HTTP path.<br/> Endpoints designated as Superadmin-only are not tenant-scoped.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import QueryFilterOperation from './QueryFilterOperation';

/**
 * The QueryFilterFieldOp model module.
 * @module model/QueryFilterFieldOp
 * @version 1.0.0
 */
class QueryFilterFieldOp {
    /**
     * Constructs a new <code>QueryFilterFieldOp</code>.
     * @alias module:model/QueryFilterFieldOp
     * @param name {String} 
     * @param value {String} 
     * @param operations {Array.<module:model/QueryFilterOperation>} 
     */
    constructor(name, value, operations) { 
        
        QueryFilterFieldOp.initialize(this, name, value, operations);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, value, operations) { 
        obj['name'] = name;
        obj['value'] = value;
        obj['operations'] = operations;
    }

    /**
     * Constructs a <code>QueryFilterFieldOp</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QueryFilterFieldOp} obj Optional instance to populate.
     * @return {module:model/QueryFilterFieldOp} The populated <code>QueryFilterFieldOp</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QueryFilterFieldOp();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
            if (data.hasOwnProperty('operations')) {
                obj['operations'] = ApiClient.convertToType(data['operations'], [QueryFilterOperation]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QueryFilterFieldOp</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QueryFilterFieldOp</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of QueryFilterFieldOp.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }
        if (data['operations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['operations'])) {
                throw new Error("Expected the field `operations` to be an array in the JSON data but got " + data['operations']);
            }
            // validate the optional field `operations` (array)
            for (const item of data['operations']) {
                QueryFilterOperation.validateJSON(item);
            };
        }

        return true;
    }


}

QueryFilterFieldOp.RequiredProperties = ["name", "value", "operations"];

/**
 * @member {String} name
 */
QueryFilterFieldOp.prototype['name'] = undefined;

/**
 * @member {String} value
 */
QueryFilterFieldOp.prototype['value'] = undefined;

/**
 * @member {Array.<module:model/QueryFilterOperation>} operations
 */
QueryFilterFieldOp.prototype['operations'] = undefined;






export default QueryFilterFieldOp;

